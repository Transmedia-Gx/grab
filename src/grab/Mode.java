package grab;

/**
 * Represents how the user trigger exporting
 */
public enum Mode {
	/**
     * Use keyboard to trigger exporting (p: PDF, j: JPG)
     */
	KEYBOARD,
	
	/**
     * Use mouse to trigger exporting (left click: PDF, right click: JPG)
     */
	MOUSE
}
